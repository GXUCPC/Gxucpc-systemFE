import {createStore} from 'vuex'

export default createStore({
    state: {
        // 后端地址
        // backURL: 'http://localhost:80/api'
        backURL: '/api'
    },
    mutations: {},
    actions: {},
    modules: {}
})
